const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			colors: {
				page: {
					"main": '#FCFE03',
				},
			},
		},
	},

	plugins: []
};

module.exports = config;
